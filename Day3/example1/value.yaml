replicaCount: 1

image:
  repository: quay.io/coreos/example-app
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1.0"

service:
  type: NodePort
  # port: 80